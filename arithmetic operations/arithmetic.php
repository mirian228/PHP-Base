<?php
    // Arithmetic operators

    $x = 10;
    $y = 2;
    $z = null;
    $counter = 0;

   // $z = $x + $y;
   // $z = $x - $y;
   // $z = $x * $y;
   // $z = $x / $y;
   // $z = $x ** $y;
   // $z = $x % $y;
   // echo "{$z}";
    
   // $counter++5;
   // $counter--5;
   // $counter+=5;
   // $counter-=5;
   echo "{$counter}";

   // Operator precedence
   // ()
   // **
   // * / %
   // +-

   $total = 1 +2 - 3 * 4 / 5 ** 6;
   echo $total;
?>